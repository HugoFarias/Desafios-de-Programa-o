#include <iostream>

using namespace std;

int main(){
	int elems, turns;
	cin >> elems >> turns;
	while(elems != 0 || turns != 0){
		int inters[turn];
		for (int i = 0; i < turns; ++i)
			cin >> inters[i];

		cin >> elems >> turns;
	}
	return 0;
}